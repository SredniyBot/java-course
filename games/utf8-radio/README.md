# 22. Радио UTF-8

Соберите байты сообщения в правильном порядке. Пройдите 5 передач.

## Learning outcomes
Строгое декодирование на внешней границе. Java: byte[], кодировки, hex, декодирование. После работы студент умеет объяснить контракт и подтвердить его тестом, а не только показать работающий экран.

## Prerequisites
Базовый синтаксис Java, методы и запуск Maven. Предыдущие игры/знания: 21. Уровень 3/3, ориентир 90–120 минут без установки JDK.

## Milestones
1. Напишите короткую design note: типы данных, границы ответственности, альтернативы.
2. Зафиксируйте Utf8Codec как адаптер для готового UI; внутренние классы, value objects и API проектируйте сами.
3. Реализуйте контракт и защитите выбор мутабельности; добавьте свои независимые тесты.
4. Обязательная защита: реализуйте запрос на изменение ниже и объясните, какие тесты защищают старое поведение.

## Контракты заданий
WHAT — требуемый результат; contract — поведение и изменения; constraints — допустимые входы. Предусловия задаёт вызывающий код: вне явно описанного домена поведение не специфицировано, скрытые тесты не требуют придуманной валидации. Null запрещён, кроме явно разрешённых элементов history. Входы не изменять, если контракт прямо не требует мутацию. Нельзя использовать время, случайность, I/O или глобальное изменяемое состояние в Utf8Codec.

### `int toUnsigned(byte value)`

- **WHAT / contract:** Представить byte как целое 0…255.
- **Constraints:** Любое значение byte от -128 до 127. Требуемая сложность: O(1).
- **Examples:** toUnsigned((byte)-1) → 255; toUnsigned((byte)-128) → 128; toUnsigned((byte)0) → 0.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `toUnsigned`, не совпадающий с examples.
- **Typical pitfalls:** Обычный cast в int сохраняет знак.

### `String decodePrefix(byte[] packet,int count)`

- **WHAT / contract:** Строго декодировать первые count байтов UTF-8. При CharacterCodingException вернуть строку <ошибка>. Остаток массива не читать.
- **Constraints:** packet не null; 0≤count≤packet.length. При некорректном/обрезанном UTF-8 вернуть ровно <ошибка>. Требуемая сложность: O(count).
- **Examples:** decodePrefix([0x41,0xFF],1) → "A"; count=2 → "<ошибка>"; count=0 → ""; одиночный 0xD0 → "<ошибка>".
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `decodePrefix`, не совпадающий с examples.
- **Typical pitfalls:** new String заменяет ошибочные байты без отказа; хвост после count не декодировать.

## Hints
<details><summary>Открыть подсказку</summary>

Готовая сигнатура — граница совместимости с UI. Она не определяет вашу внутреннюю архитектуру. Обоснуйте собственные типы и API в DESIGN.md; избегайте общего «движка на все случаи».

</details>

## Tests и запуск
Из корня репозитория: `./mvnw -pl :utf8-radio -am test`. Windows: `.\mvnw.cmd -pl :utf8-radio -am test`.
Проверки находятся в `src/test/java/edu/course/games/utf8radio/domain`. Это Jupiter, не отдельный main.
После полной сборки `./mvnw verify` запустите `java -jar app/target/java-games-app-1.0.0.jar 22`.
В IDE запустите `edu.course.app.Launcher` с аргументом `22`. F5 сбрасывает игру, F6 ставит на паузу; код после правки нужно перекомпилировать.
В студенческой выдаче тесты изначально красные из-за незаполненных методов. Это ожидаемо; отключать их нельзя.

## Обязательный design milestone и защита
Заменить строку-маркер на sealed DecodeResult: Success или Failure с позицией.
Сохраните существующие контракты через адаптер либо документируйте согласованное изменение и обновите UI с тестами. Полная эталонная версия закрывает базовые контракты; этот запрос студент выполняет самостоятельно.

## AI policy / check questions
Можно использовать AI для объяснения API, поиска гипотез и критики тестов. В `AI_USAGE.md` укажите инструмент, задачу, что принято и как независимо проверено (или «не использовался»). Не засчитывается тест, чей expected просто скопирован из реализации.

1. Объясните `toUnsigned` на входе, которого нет в примерах.
2. Какой объект изменяется, кто ещё может держать ссылку на него?
3. Какой тест упадёт при ошибке: Обычный cast в int сохраняет знак.
4. Выполните небольшой change request без повторной генерации всего класса.

## Путеводитель по коду

- [`Utf8Codec`](src/main/java/edu/course/games/utf8radio/domain/Utf8Codec.java): контракты и учебные методы, без UI.
- [`Utf8RadioGame`](src/main/java/edu/course/games/utf8radio/ui/Utf8RadioGame.java): перевод событий и анимация.
- [`Utf8RadioRenderer`](src/main/java/edu/course/games/utf8radio/ui/Utf8RadioRenderer.java): только рисование, без изменения состояния.
- `src/test/java/edu/course/games/utf8radio/domain`: примеры и граничные тесты, зеркалящие domain.
