# 18. Лазерная почта — Письмо в зеркальном цикле

Луч попал в замкнутый коридор. Лимит кадров скрывает проблему и обрывает правильный путь.

## Что вы исследуете

Состояние — клетка и направление. Повтор клетки с другим направлением не доказывает цикл.

Темы: State record, Set, завершение. Для доказательства корректности используйте понятия лекции 3: контракт, класс входов, независимый oracle, граница и регрессия. Формальные доказательства не требуются; требуется объяснить основание каждого важного assertion.

## Перед началом

Предыдущая работа: [17. Склад лунного сыра](../moon-sokoban/README.md). Переносите её практику проверки, а не её код.

Работа 18/25. Ориентир 300–330 минут. До реализации согласуйте контракт нового API в DESIGN.md. Обязательны модель, интеграция, проверки отказов и разбор компромиссов.

## Обязательная работа: Трассировка

1. Запустите демо и опишите наблюдаемый дефект: действие → результат → ожидаемое поведение. Баннер показывает вызванные незавершённые методы, а не процент готовности.
2. Реализуйте базовые методы по контрактам ниже. Заглушку ExercisePreview.unfinished заменяйте целиком; не включайте preview внутри acceptance-тестов.
3. Добавьте trace(grid,start,direction) с EXIT/TARGET/LOOP и immutable путём. В клетке сначала проверяйте повтор состояния, затем цель, отражение и шаг. Сетка неизменна; максимум 4×h×w состояний. Символ цели и валидацию опишите до кода; подключите UI.
4. Проверьте выход, оба зеркала с четырёх сторон, цель до петли, цикл, повтор клетки в другом направлении. Различите Set<Cell> и Set<RayState>.
5. Повторите исходный сценарий в окне. Отдельно проверьте F5, F6 и ошибочный ввод. Сохраните команду проверки и её результат.

## Что сдавать

- Код, собственные Jupiter-тесты и небольшой TEST_PLAN.md: класс входов → пример → ожидаемый результат → какую ошибку ловит.
- Один минимальный контрпример к правдоподобной неправильной реализации. Сначала покажите красный тест, затем исправление.
- REGRESSION.md: вход, причина дефекта, почему исправление устраняет причину, ограничения проверки.
- Новый domain API и его использование в UI; изолированный класс без подключения не завершает работу.
- DESIGN.md: инварианты, владельцы данных, rejected alternative и стоимость операций. Один новый change request выполните после review.

## Приёмка

Базовые публичные тесты обязательны, но не исчерпывают работу. Новая часть оценивается по условиям пункта 3 и доказательствам пункта 4. Зелёные tests без обязательной новой функциональности не означают завершение. Нельзя ослаблять существующий контракт, подгонять expected или удалять проверки ради зелёного статуса.

## Контракты заданий
WHAT — требуемый результат; contract — поведение и изменения; constraints — допустимые входы. Предусловия задаёт вызывающий код: вне явно описанного домена поведение не специфицировано, скрытые тесты не требуют придуманной валидации. Null запрещён, кроме явно разрешённых элементов history. Входы не изменять, если контракт прямо не требует мутацию. Нельзя использовать время, случайность, I/O или глобальное изменяемое состояние в BeamRules.

### `int reflectDirection(int direction,char mirror)`

- **WHAT / contract:** Направления: 0 вправо,1 вниз,2 влево,3 вверх. Для /: 0→3,1→2,2→1,3→0; для обратного слеша: 0→1,1→0,2→3,3→2. Иначе направление не менять.
- **Constraints:** direction ∈ {0,1,2,3}; mirror любой char. Требуемая сложность: O(1).
- **Examples:** reflectDirection(0,'/') → 3; reflectDirection(3,'\\') → 2; reflectDirection(2,'.') → 2.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `reflectDirection`, не совпадающий с examples.
- **Typical pitfalls:** Перепутать / и \ легко; два отражения одним зеркалом возвращают направление.

### `boolean isInsideGrid(int r,int c,int h,int w)`

- **WHAT / contract:** Проверить обе координаты: 0<=r<h и 0<=c<w.
- **Constraints:** h,w ≥0; r,c любые int. Требуемая сложность: O(1).
- **Examples:** isInsideGrid(0,0,1,1) → true; isInsideGrid(1,0,1,1) → false; isInsideGrid(0,0,0,1) → false.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `isInsideGrid`, не совпадающий с examples.
- **Typical pitfalls:** Правая/нижняя границы исключены; отрицательные координаты недопустимы.


## Подсказка

<details><summary>Открыть после собственной попытки</summary>

Состояние — клетка и направление. Повтор клетки с другим направлением не доказывает цикл. Начните с минимального входа, на котором два возможных объяснения поведения расходятся. Зафиксируйте его до реализации.

</details>

## Запуск и проверки

Из корня: ./mvnw -DskipTests package, затем java -jar app/target/java-games-app-1.0.0.jar 18.
На Windows используйте .\mvnw.cmd. Это компиляция и запуск демо, не приёмка.

Приёмка: ./mvnw -pl :laser-mail -am verify. Незавершённые методы дают IncompleteExerciseException.
При запуске окна та же заглушка даёт намеренно неполноценное поведение. Частично реализованная игра использует ваши готовые методы; ошибки вашего кода не заменяются демо.
F5 сбрасывает сцену; после изменения кода нужна пересборка. В каталоге src/test/java — настоящие Jupiter-тесты.

## Самопроверка и защита

1. Как доказать завершение без лимита 1000?
2. Какой ваш тест пройдёт у неверного решения и чем вы дополнили его?
3. Назовите предположение, вне которого ваш контракт ничего не обещает.

AI можно использовать для гипотез и review. В AI_USAGE.md укажите принятые/отклонённые предложения и независимую проверку (либо «не использовался»). На защите требуется объяснить контрпример и внести изменение самостоятельно.

## Дополнительно

После обязательной работы предложите одно изменение правил. До кода опишите, какие контракты оно затронет, и придумайте тест совместимости. Это добровольная часть.

## Путеводитель по коду

- [`BeamRules`](src/main/java/edu/course/games/lasermail/domain/BeamRules.java): контракты и учебные методы, без UI.
- [`LaserMailGame`](src/main/java/edu/course/games/lasermail/ui/LaserMailGame.java): перевод событий и анимация.
- [`LaserMailRenderer`](src/main/java/edu/course/games/lasermail/ui/LaserMailRenderer.java): только рисование, без изменения состояния.
- `src/test/java/edu/course/games/lasermail/domain`: примеры и граничные тесты, зеркалящие domain.

