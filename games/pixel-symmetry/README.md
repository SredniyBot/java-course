# 13. Мастерская симметрии — Ось посреди пикселя

Центральный столбец нечётного холста остаётся на месте. Правая половина не должна перерисовывать левую.

## Что вы исследуете

Копирование половины не является обменом половин; повторное отражение идемпотентно.

Темы: Команды, идемпотентность, инкапсуляция. Для доказательства корректности используйте понятия лекции 3: контракт, класс входов, независимый oracle, граница и регрессия. Формальные доказательства не требуются; требуется объяснить основание каждого важного assertion.

## Перед началом

Предыдущая работа: [12. Буквенный лабиринт](../word-maze/README.md). Переносите её практику проверки, а не её код.

Работа 13/25. Ориентир 225–255 минут. Кроме базовых методов обязательна новая модель с тестами и подключением к UI. Не складывайте состояние в static.

## Обязательная работа: Команда кисти с результатом

1. Запустите демо и опишите наблюдаемый дефект: действие → результат → ожидаемое поведение. Баннер показывает вызванные незавершённые методы, а не процент готовности.
2. Реализуйте базовые методы по контрактам ниже. Заглушку ExercisePreview.unfinished заменяйте целиком; не включайте preview внутри acceptance-тестов.
3. Добавьте PixelCanvas. paint(row,col,color) возвращает число реально изменённых клеток (0/1), mirror() — число изменённых справа. Снимок независим; вне границ paint возвращает 0. Подключите UI.
4. Проверьте чётную/нечётную ширину, ширину 1, повтор mirror. Дополните property идемпотентности несимметричным якорным примером.
5. Повторите исходный сценарий в окне. Отдельно проверьте F5, F6 и ошибочный ввод. Сохраните команду проверки и её результат.

## Что сдавать

- Код, собственные Jupiter-тесты и небольшой TEST_PLAN.md: класс входов → пример → ожидаемый результат → какую ошибку ловит.
- Один минимальный контрпример к правдоподобной неправильной реализации. Сначала покажите красный тест, затем исправление.
- REGRESSION.md: вход, причина дефекта, почему исправление устраняет причину, ограничения проверки.
- Новый domain API и его использование в UI; изолированный класс без подключения не завершает работу.

## Приёмка

Базовые публичные тесты обязательны, но не исчерпывают работу. Новая часть оценивается по условиям пункта 3 и доказательствам пункта 4. Зелёные tests без обязательной новой функциональности не означают завершение. Нельзя ослаблять существующий контракт, подгонять expected или удалять проверки ради зелёного статуса.

## Контракты заданий
WHAT — требуемый результат; contract — поведение и изменения; constraints — допустимые входы. Предусловия задаёт вызывающий код: вне явно описанного домена поведение не специфицировано, скрытые тесты не требуют придуманной валидации. Null запрещён, кроме явно разрешённых элементов history. Входы не изменять, если контракт прямо не требует мутацию. Нельзя использовать время, случайность, I/O или глобальное изменяемое состояние в SymmetryRules.

### `void paintCell(int[][] image,int row,int col,int color)`

- **WHAT / contract:** Поставить цвет в клетку. Если индексы вне матрицы, ничего не менять.
- **Constraints:** image не null; строки не null, прямоугольность не обязательна; row,col любые int; color любой int. Требуемая сложность: O(1).
- **Examples:** paintCell([[1,2]],0,1,9) → [[1,9]]; row=-1 или col=2 → без изменений.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `paintCell`, не совпадающий с examples.
- **Typical pitfalls:** Выход за край здесь штатный no-op, а не исключение.

### `void mirrorLeftToRight(int[][] image)`

- **WHAT / contract:** Скопировать левую половину в правую, отражая относительно вертикальной оси. Нечётную центральную колонку сохранить.
- **Constraints:** Строки не null и разные объекты; допустимы разная длина и пустые строки. Требуемая сложность: O(числа элементов), O(1) доп. памяти.
- **Examples:** mirrorLeftToRight([[1,2,3,4,5]]) → [[1,2,3,2,1]].
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `mirrorLeftToRight`, не совпадающий с examples.
- **Typical pitfalls:** Копировать слева направо только половину; среднюю колонку не менять.

### `boolean isHorizontallySymmetric(int[][] image)`

- **WHAT / contract:** Проверить горизонтальное отражение в каждой строке. Не менять матрицу.
- **Constraints:** image и строки не null, в том числе пустые и разной длины. Требуемая сложность: O(числа элементов).
- **Examples:** isHorizontallySymmetric([[1,2,1],[]]) → true; isHorizontallySymmetric([[1,2]]) → false.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `isHorizontallySymmetric`, не совпадающий с examples.
- **Typical pitfalls:** Проверка ничего не исправляет; пустая строка симметрична.


## Подсказка

<details><summary>Открыть после собственной попытки</summary>

Копирование половины не является обменом половин; повторное отражение идемпотентно. Начните с минимального входа, на котором два возможных объяснения поведения расходятся. Зафиксируйте его до реализации.

</details>

## Запуск и проверки

Из корня: ./mvnw -DskipTests package, затем java -jar app/target/java-games-app-1.0.0.jar 13.
На Windows используйте .\mvnw.cmd. Это компиляция и запуск демо, не приёмка.

Приёмка: ./mvnw -pl :pixel-symmetry -am verify. Незавершённые методы дают IncompleteExerciseException.
При запуске окна та же заглушка даёт намеренно неполноценное поведение. Частично реализованная игра использует ваши готовые методы; ошибки вашего кода не заменяются демо.
F5 сбрасывает сцену; после изменения кода нужна пересборка. В каталоге src/test/java — настоящие Jupiter-тесты.

## Самопроверка и защита

1. Чем mirror отличается от reverse строки?
2. Какой ваш тест пройдёт у неверного решения и чем вы дополнили его?
3. Назовите предположение, вне которого ваш контракт ничего не обещает.

AI можно использовать для гипотез и review. В AI_USAGE.md укажите принятые/отклонённые предложения и независимую проверку (либо «не использовался»). На защите требуется объяснить контрпример и внести изменение самостоятельно.

## Дополнительно

После обязательной работы предложите одно изменение правил. До кода опишите, какие контракты оно затронет, и придумайте тест совместимости. Это добровольная часть.

## Путеводитель по коду

- [`SymmetryRules`](src/main/java/edu/course/games/pixelsymmetry/domain/SymmetryRules.java): контракты и учебные методы, без UI.
- [`PixelSymmetryGame`](src/main/java/edu/course/games/pixelsymmetry/ui/PixelSymmetryGame.java): перевод событий и анимация.
- [`PixelSymmetryRenderer`](src/main/java/edu/course/games/pixelsymmetry/ui/PixelSymmetryRenderer.java): только рисование, без изменения состояния.
- `src/test/java/edu/course/games/pixelsymmetry/domain`: примеры и граничные тесты, зеркалящие domain.

