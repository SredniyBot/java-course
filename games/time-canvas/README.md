# 19. Холст времени — Один мазок — одна отмена

Мазок по десяти клеткам должен отменяться целиком. Новый мазок после undo удаляет ветку redo.

## Что вы исследуете

История хранит значения, а не ссылки. Граница транзакции важнее события мыши.

Темы: История, транзакции, модельные тесты. Для доказательства корректности используйте понятия лекции 3: контракт, класс входов, независимый oracle, граница и регрессия. Формальные доказательства не требуются; требуется объяснить основание каждого важного assertion.

## Перед началом

Предыдущая работа: [18. Лазерная почта](../laser-mail/README.md). Переносите её практику проверки, а не её код.

Работа 19/25. Ориентир 315–345 минут. До реализации согласуйте контракт нового API в DESIGN.md. Обязательны модель, интеграция, проверки отказов и разбор компромиссов.

## Обязательная работа: Транзакция мазка

1. Запустите демо и опишите наблюдаемый дефект: действие → результат → ожидаемое поведение. Баннер показывает вызванные незавершённые методы, а не процент готовности.
2. Реализуйте базовые методы по контрактам ниже. Заглушку ExercisePreview.unfinished заменяйте целиком; не включайте preview внутри acceptance-тестов.
3. Расширьте PixelHistory: beginStroke, paint, commitStroke, cancelStroke. До commit currentPixels прежний; commit создаёт максимум один снимок, пустой мазок не создаёт. Вложенный begin, commit без begin и undo во время мазка — IllegalStateException без изменений. cancel отбрасывает изменения. Подключите нажатие/отпускание мыши.
4. Проверьте ветвление, cancel, пустой commit, лимит истории и мутацию переданного массива. Сдайте state machine.
5. Повторите исходный сценарий в окне. Отдельно проверьте F5, F6 и ошибочный ввод. Сохраните команду проверки и её результат.

## Что сдавать

- Код, собственные Jupiter-тесты и небольшой TEST_PLAN.md: класс входов → пример → ожидаемый результат → какую ошибку ловит.
- Один минимальный контрпример к правдоподобной неправильной реализации. Сначала покажите красный тест, затем исправление.
- REGRESSION.md: вход, причина дефекта, почему исправление устраняет причину, ограничения проверки.
- Новый domain API и его использование в UI; изолированный класс без подключения не завершает работу.
- DESIGN.md: инварианты, владельцы данных, rejected alternative и стоимость операций. Один новый change request выполните после review.

## Приёмка

Базовые публичные тесты обязательны, но не исчерпывают работу. Новая часть оценивается по условиям пункта 3 и доказательствам пункта 4. Зелёные tests без обязательной новой функциональности не означают завершение. Нельзя ослаблять существующий контракт, подгонять expected или удалять проверки ради зелёного статуса.

## Контракты заданий
WHAT — требуемый результат; contract — поведение и изменения; constraints — допустимые входы. Предусловия задаёт вызывающий код: вне явно описанного домена поведение не специфицировано, скрытые тесты не требуют придуманной валидации. Null запрещён, кроме явно разрешённых элементов history. Входы не изменять, если контракт прямо не требует мутацию. Нельзя использовать время, случайность, I/O или глобальное изменяемое состояние в CanvasRules.

### `int[][] copyPixels(int[][] a)`

- **WHAT / contract:** Вернуть независимую копию прямоугольной матрицы int, включая копии всех строк.
- **Constraints:** a и строки не null; пустые и неодинаковые по длине строки допустимы. Требуемая сложность: O(числа элементов) времени/памяти.
- **Examples:** copyPixels([[1],[2,3]]) → равные значения; изменение copyPixels[0][0] не меняет a[0][0].
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `copyPixels`, не совпадающий с examples.
- **Typical pitfalls:** Копирование внешнего массива не копирует строки; даже строки длины 0 должны быть отдельными.

### `int countDifferences(int[][] a,int[][] b)`

- **WHAT / contract:** Посчитать позиции с разными значениями. Размеры совпадают.
- **Constraints:** a,b и строки не null; одинаковое число строк и попарные длины строк. Требуемая сложность: O(числа элементов).
- **Examples:** countDifferences([[1,2]],[[1,3]]) → 1; равные матрицы → 0.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `countDifferences`, не совпадающий с examples.
- **Typical pitfalls:** Сравнивать значения клеток, не ссылки; обе матрицы сохранить.

### `boolean hasPreviousSnapshot(int index,int[][][] snapshots)`

- **WHAT / contract:** Вернуть true, если index>0 и предыдущий снимок не null. В новом массиве ссылок неиспользованные элементы равны null. Индекс внутри snapshots.
- **Constraints:** snapshots не null; 0≤index<snapshots.length; элементы snapshots могут быть null. Требуемая сложность: O(1).
- **Examples:** hasPreviousSnapshot(0,[null]) → false; hasPreviousSnapshot(1,[[[1]],null]) → true; hasPreviousSnapshot(1,[null,null]) → false.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `hasPreviousSnapshot`, не совпадающий с examples.
- **Typical pitfalls:** Не читать index-1 до проверки index>0; текущее значение snapshots[index] не определяет возможность undo.


## Подсказка

<details><summary>Открыть после собственной попытки</summary>

История хранит значения, а не ссылки. Граница транзакции важнее события мыши. Начните с минимального входа, на котором два возможных объяснения поведения расходятся. Зафиксируйте его до реализации.

</details>

## Запуск и проверки

Из корня: ./mvnw -DskipTests package, затем java -jar app/target/java-games-app-1.0.0.jar 19.
На Windows используйте .\mvnw.cmd. Это компиляция и запуск демо, не приёмка.

Приёмка: ./mvnw -pl :time-canvas -am verify. Незавершённые методы дают IncompleteExerciseException.
При запуске окна та же заглушка даёт намеренно неполноценное поведение. Частично реализованная игра использует ваши готовые методы; ошибки вашего кода не заменяются демо.
F5 сбрасывает сцену; после изменения кода нужна пересборка. В каталоге src/test/java — настоящие Jupiter-тесты.

## Самопроверка и защита

1. Чем отсутствие изменений отличается от возвращения к исходному цвету?
2. Какой ваш тест пройдёт у неверного решения и чем вы дополнили его?
3. Назовите предположение, вне которого ваш контракт ничего не обещает.

AI можно использовать для гипотез и review. В AI_USAGE.md укажите принятые/отклонённые предложения и независимую проверку (либо «не использовался»). На защите требуется объяснить контрпример и внести изменение самостоятельно.

## Дополнительно

После обязательной работы предложите одно изменение правил. До кода опишите, какие контракты оно затронет, и придумайте тест совместимости. Это добровольная часть.

## Путеводитель по коду

- [`CanvasRules`](src/main/java/edu/course/games/timecanvas/domain/CanvasRules.java): контракты и учебные методы, без UI.
- [`TimeCanvasGame`](src/main/java/edu/course/games/timecanvas/ui/TimeCanvasGame.java): перевод событий и анимация.
- [`TimeCanvasRenderer`](src/main/java/edu/course/games/timecanvas/ui/TimeCanvasRenderer.java): только рисование, без изменения состояния.
- `src/test/java/edu/course/games/timecanvas/domain`: примеры и граничные тесты, зеркалящие domain.
- [`PixelHistory`](src/main/java/edu/course/games/timecanvas/domain/PixelHistory.java): владелец игрового состояния, отдельно проверяемый без окна.

