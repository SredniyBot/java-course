# 16. Котики 256 — Кот, который слился дважды

Ряд 2,2,4,0 превращают в 8, хотя новая плитка не имеет права сливаться повторно.

## Что вы исследуете

Сжатие, слияние и spawn — разные фазы. No-op не расходует случайность.

Темы: records, фазы, управляемая случайность. Для доказательства корректности используйте понятия лекции 3: контракт, класс входов, независимый oracle, граница и регрессия. Формальные доказательства не требуются; требуется объяснить основание каждого важного assertion.

## Перед началом

Предыдущая работа: [15. Ночной город](../lights-out/README.md). Переносите её практику проверки, а не её код.

Работа 16/25. Ориентир 270–300 минут. До реализации согласуйте контракт нового API в DESIGN.md. Обязательны модель, интеграция, проверки отказов и разбор компромиссов.

## Обязательная работа: Ход как результат

1. Запустите демо и опишите наблюдаемый дефект: действие → результат → ожидаемое поведение. Баннер показывает вызванные незавершённые методы, а не процент готовности.
2. Реализуйте базовые методы по контрактам ниже. Заглушку ExercisePreview.unfinished заменяйте целиком; не включайте preview внутри acceptance-тестов.
3. Добавьте MoveResult: changed, gainedScore, before, after. Счёт — сумма созданных слиянием плиток. No-op даёт 0 и не запрашивает spawn. Снимки независимы. Сохраните атомарность TileBoard при overflow; подключите счёт к UI.
4. Таблица 2,2,2,2; 2,2,4,0; 0,2,0,2. Проверка суммы до spawn и всех четырёх направлений по независимым ожидаемым доскам.
5. Повторите исходный сценарий в окне. Отдельно проверьте F5, F6 и ошибочный ввод. Сохраните команду проверки и её результат.

## Что сдавать

- Код, собственные Jupiter-тесты и небольшой TEST_PLAN.md: класс входов → пример → ожидаемый результат → какую ошибку ловит.
- Один минимальный контрпример к правдоподобной неправильной реализации. Сначала покажите красный тест, затем исправление.
- REGRESSION.md: вход, причина дефекта, почему исправление устраняет причину, ограничения проверки.
- Новый domain API и его использование в UI; изолированный класс без подключения не завершает работу.
- DESIGN.md: инварианты, владельцы данных, rejected alternative и стоимость операций. Один новый change request выполните после review.

## Приёмка

Базовые публичные тесты обязательны, но не исчерпывают работу. Новая часть оценивается по условиям пункта 3 и доказательствам пункта 4. Зелёные tests без обязательной новой функциональности не означают завершение. Нельзя ослаблять существующий контракт, подгонять expected или удалять проверки ради зелёного статуса.

## Контракты заданий
WHAT — требуемый результат; contract — поведение и изменения; constraints — допустимые входы. Предусловия задаёт вызывающий код: вне явно описанного домена поведение не специфицировано, скрытые тесты не требуют придуманной валидации. Null запрещён, кроме явно разрешённых элементов history. Входы не изменять, если контракт прямо не требует мутацию. Нельзя использовать время, случайность, I/O или глобальное изменяемое состояние в TileRules.

### `int[] mergeLeft(int[] row)`

- **WHAT / contract:** Вернуть новый ряд: убрать нули, слить соседние равные числа один раз слева направо, дополнить нулями. Оригинал не менять.
- **Constraints:** row не null; элементы 0 либо степени 2 ≤2^29; пустой ряд допустим. Требуемая сложность: O(n) времени/памяти.
- **Examples:** mergeLeft([2,2,2,2]) → [4,4,0,0]; mergeLeft([2,2,4,0]) → [4,4,0,0]; mergeLeft([2,0,2]) → [4,0,0].
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `mergeLeft`, не совпадающий с examples.
- **Typical pitfalls:** Полученный результат не сливается повторно в том же ходе; вход должен остаться прежним.

### `int[][] rotateClockwise(int[][] a)`

- **WHAT / contract:** Новая квадратная матрица, повёрнутая по часовой стрелке.
- **Constraints:** Квадратная матрица, строки не null; допустима матрица 0×0. Требуемая сложность: O(n²) времени/памяти.
- **Examples:** rotateClockwise([[1,2],[3,4]]) → [[3,1],[4,2]].
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `rotateClockwise`, не совпадающий с examples.
- **Typical pitfalls:** Это новый снимок; четыре поворота восстанавливают значения, но не ссылки.


## Подсказка

<details><summary>Открыть после собственной попытки</summary>

Сжатие, слияние и spawn — разные фазы. No-op не расходует случайность. Начните с минимального входа, на котором два возможных объяснения поведения расходятся. Зафиксируйте его до реализации.

</details>

## Запуск и проверки

Из корня: ./mvnw -DskipTests package, затем java -jar app/target/java-games-app-1.0.0.jar 16.
На Windows используйте .\mvnw.cmd. Это компиляция и запуск демо, не приёмка.

Приёмка: ./mvnw -pl :cats-256 -am verify. Незавершённые методы дают IncompleteExerciseException.
При запуске окна та же заглушка даёт намеренно неполноценное поведение. Частично реализованная игра использует ваши готовые методы; ошибки вашего кода не заменяются демо.
F5 сбрасывает сцену; после изменения кода нужна пересборка. В каталоге src/test/java — настоящие Jupiter-тесты.

## Самопроверка и защита

1. Почему seed слабее проверки числа вызовов генератора?
2. Какой ваш тест пройдёт у неверного решения и чем вы дополнили его?
3. Назовите предположение, вне которого ваш контракт ничего не обещает.

AI можно использовать для гипотез и review. В AI_USAGE.md укажите принятые/отклонённые предложения и независимую проверку (либо «не использовался»). На защите требуется объяснить контрпример и внести изменение самостоятельно.

## Дополнительно

После обязательной работы предложите одно изменение правил. До кода опишите, какие контракты оно затронет, и придумайте тест совместимости. Это добровольная часть.

## Путеводитель по коду

- [`TileRules`](src/main/java/edu/course/games/cats256/domain/TileRules.java): контракты и учебные методы, без UI.
- [`Cats256Game`](src/main/java/edu/course/games/cats256/ui/Cats256Game.java): перевод событий и анимация.
- [`Cats256Renderer`](src/main/java/edu/course/games/cats256/ui/Cats256Renderer.java): только рисование, без изменения состояния.
- `src/test/java/edu/course/games/cats256/domain`: примеры и граничные тесты, зеркалящие domain.
- [`TileBoard`](src/main/java/edu/course/games/cats256/domain/TileBoard.java): владелец игрового состояния, отдельно проверяемый без окна.

