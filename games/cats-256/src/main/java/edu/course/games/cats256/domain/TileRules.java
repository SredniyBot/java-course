package edu.course.games.cats256.domain;

/** Котики 256: операции над игровыми данными без интерфейса. */
public final class TileRules {
  private TileRules() {}

  /**
   * Выполняет один сдвиг плиток влево: убирает промежутки, объединяет соседние равные плитки и
   * дополняет результат нулями справа. Полученная при слиянии плитка не сливается повторно в этом
   * ходе. Исходный массив не изменяется.
   *
   * <p>row не null; элементы 0 либо степени 2 ≤2^29; пустой ряд допустим.
   *
   * @param row строка плиток; 0 означает свободное место
   * @return новая строка той же длины после одного сдвига и слияний
   */
  public static int[] mergeLeft(int[] row) {
    // TODO cats-256.mergeLeft: реализуйте действие по контракту выше.
    return edu.course.learning.ExercisePreview.unfinished("cats-256.mergeLeft", () -> row.clone());
  }

  /**
   * Возвращает повёрнутую на 90° по часовой стрелке матрицу. Исходная матрица не изменяется, строки
   * результата не разделяют с ней память.
   *
   * <p>Квадратная матрица, строки не null; допустима матрица 0×0.
   *
   * @param a исходная матрица; строки не равны null
   * @return новая матрица без общих строк с исходной
   */
  public static int[][] rotateClockwise(int[][] a) {
    int n = a.length;
    int[][] b = new int[n][n];
    for (int r = 0; r < n; r++) for (int c = 0; c < n; c++) b[c][n - 1 - r] = a[r][c];
    return b;
  }
}
