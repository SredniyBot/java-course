# 14. Цветной прилив

Залейте всё поле одним цветом за 24 хода, начиная сверху слева.

## Learning outcomes
Связность и очередь без повторного обхода. Java: Обход матрицы, очередь из массивов. После работы студент умеет объяснить контракт и подтвердить его тестом, а не только показать работающий экран.

## Prerequisites
Базовый синтаксис Java, методы и запуск Maven. Предыдущие игры/знания: 07,13. Уровень 2/3, ориентир 120–150 минут без установки JDK.

## Milestones
1. До реализации составьте таблицу граничных случаев и определите владельца массивов.
2. Реализуйте методы с соблюдением контрактов; самостоятельно выберите цикл и вспомогательные методы.
3. Добавьте тест отсутствия лишних изменений и один параметризованный тест.
4. Объясните сложность и продемонстрируйте игру.

## Контракты заданий
WHAT — требуемый результат; contract — поведение и изменения; constraints — допустимые входы. Предусловия задаёт вызывающий код: вне явно описанного домена поведение не специфицировано, скрытые тесты не требуют придуманной валидации. Null запрещён, кроме явно разрешённых элементов history. Входы не изменять, если контракт прямо не требует мутацию. Нельзя использовать время, случайность, I/O или глобальное изменяемое состояние в FloodRules.

### `void fillFromOrigin(int[][] a,int color)`

- **WHAT / contract:** Перекрасить компоненту клетки [0][0] по четырём сторонам. Использовать очередь из двух int[] размером h w. Помечать клетку новым цветом ДО добавления. Если цвет не меняется, сразу выйти.
- **Constraints:** Непустая прямоугольная матрица width>0; h*w ≤ 10^6; строки разные; любой int цвет. Требуемая сложность: O(h*w) времени/памяти.
- **Examples:** fillFromOrigin([[1,2],[2,1]],9) → [[9,2],[2,1]]; color=1 ничего не меняет.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `fillFromOrigin`, не совпадающий с examples.
- **Typical pitfalls:** Диагональ не связана. Отмечайте посещение до добавления в очередь; тот же цвет — немедленный выход.

### `boolean isUniform(int[][] a)`

- **WHAT / contract:** Все значения равны a[0][0]. Матрица непустая.
- **Constraints:** Непустая прямоугольная матрица width>0. Требуемая сложность: O(h*w).
- **Examples:** isUniform([[2,2],[2,2]]) → true; isUniform([[2,3]]) → false.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `isUniform`, не совпадающий с examples.
- **Typical pitfalls:** Не требуются определённые цвета, только равенство; вход сохраняется.

## Hints
<details><summary>Открыть подсказку</summary>

Перед циклом запишите инвариант: какая часть входа уже обработана и что можно перезаписать. Для нового массива отдельно проверьте, что он не разделяет изменяемые строки со входом.

</details>

## Tests и запуск
Из корня репозитория: `./mvnw -pl :color-flood -am test`. Windows: `.\mvnw.cmd -pl :color-flood -am test`.
Проверки находятся в `src/test/java/edu/course/games/colorflood/domain`. Это Jupiter, не отдельный main.
После полной сборки `./mvnw verify` запустите `java -jar app/target/java-games-app-1.0.0.jar 14`.
В IDE запустите `edu.course.app.Launcher` с аргументом `14`. F5 сбрасывает игру, F6 ставит на паузу; код после правки нужно перекомпилировать.
В студенческой выдаче тесты изначально красные из-за незаполненных методов. Это ожидаемо; отключать их нельзя.

## Optional extension / запрос на защите
Выбирать любую стартовую клетку и вернуть число перекрашенных клеток.
Сохраните существующие контракты через адаптер либо документируйте согласованное изменение и обновите UI с тестами. Полная эталонная версия закрывает базовые контракты; этот запрос студент выполняет самостоятельно.

## AI policy / check questions
Можно использовать AI для объяснения API, поиска гипотез и критики тестов. В `AI_USAGE.md` укажите инструмент, задачу, что принято и как независимо проверено (или «не использовался»). Не засчитывается тест, чей expected просто скопирован из реализации.

1. Объясните `fillFromOrigin` на входе, которого нет в примерах.
2. Какой объект изменяется, кто ещё может держать ссылку на него?
3. Какой тест упадёт при ошибке: Диагональ не связана. Отмечайте посещение до добавления в очередь; тот же цвет — немедленный выход.
4. Выполните небольшой change request без повторной генерации всего класса.

## Путеводитель по коду

- [`FloodRules`](src/main/java/edu/course/games/colorflood/domain/FloodRules.java): контракты и учебные методы, без UI.
- [`ColorFloodGame`](src/main/java/edu/course/games/colorflood/ui/ColorFloodGame.java): перевод событий и анимация.
- [`ColorFloodRenderer`](src/main/java/edu/course/games/colorflood/ui/ColorFloodRenderer.java): только рисование, без изменения состояния.
- `src/test/java/edu/course/games/colorflood/domain`: примеры и граничные тесты, зеркалящие domain.
