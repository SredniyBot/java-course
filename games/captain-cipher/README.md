# 23. Шифр капитана — Сдвиг длиной в int

Ключ 3 работает, Integer.MAX_VALUE ломает шифр. Round-trip подтверждает две одинаковые ошибки.

## Что вы исследуете

Сложение до остатка переполняется; отрицание MIN_VALUE тоже не помещается в int.

Темы: Overflow, floorMod, версионирование. Для доказательства корректности используйте понятия лекции 3: контракт, класс входов, независимый oracle, граница и регрессия. Формальные доказательства не требуются; требуется объяснить основание каждого важного assertion.

## Перед началом

Предыдущая работа: [22. Радио UTF-8](../utf8-radio/README.md). Переносите её практику проверки, а не её код.

Работа 23/25. Ориентир 375–405 минут. До реализации согласуйте контракт нового API в DESIGN.md. Обязательны модель, интеграция, проверки отказов и разбор компромиссов.

## Обязательная работа: Версионируемый пакет

1. Запустите демо и опишите наблюдаемый дефект: действие → результат → ожидаемое поведение. Баннер показывает вызванные незавершённые методы, а не процент готовности.
2. Реализуйте базовые методы по контрактам ниже. Заглушку ExercisePreview.unfinished заменяйте целиком; не включайте preview внутри acceptance-тестов.
3. Добавьте CipherPacket(version,offset,text) и codec. Формат документируйте до кода. Версия 1 сдвигает только A–Z, остальные char сохраняет. Неизвестная версия/повреждённый заголовок отклоняются без частичного результата. Любой int-ключ. Подключите импорт/экспорт к UI.
4. Проверьте MIN/MAX, фиксированный вектор, round-trip. Оракул использует long. Формат однозначно хранит разделители и переносы строк; сдайте совместимый пример.
5. Повторите исходный сценарий в окне. Отдельно проверьте F5, F6 и ошибочный ввод. Сохраните команду проверки и её результат.

## Что сдавать

- Код, собственные Jupiter-тесты и небольшой TEST_PLAN.md: класс входов → пример → ожидаемый результат → какую ошибку ловит.
- Один минимальный контрпример к правдоподобной неправильной реализации. Сначала покажите красный тест, затем исправление.
- REGRESSION.md: вход, причина дефекта, почему исправление устраняет причину, ограничения проверки.
- Новый domain API и его использование в UI; изолированный класс без подключения не завершает работу.
- DESIGN.md: инварианты, владельцы данных, rejected alternative и стоимость операций. Один новый change request выполните после review.
- Воспроизводимый сценарий из нескольких команд и независимая модель/оракул на ограниченном подмножестве входов.

## Приёмка

Базовые публичные тесты обязательны, но не исчерпывают работу. Новая часть оценивается по условиям пункта 3 и доказательствам пункта 4. Зелёные tests без обязательной новой функциональности не означают завершение. Нельзя ослаблять существующий контракт, подгонять expected или удалять проверки ради зелёного статуса.

## Контракты заданий
WHAT — требуемый результат; contract — поведение и изменения; constraints — допустимые входы. Предусловия задаёт вызывающий код: вне явно описанного домена поведение не специфицировано, скрытые тесты не требуют придуманной валидации. Null запрещён, кроме явно разрешённых элементов history. Входы не изменять, если контракт прямо не требует мутацию. Нельзя использовать время, случайность, I/O или глобальное изменяемое состояние в CaesarCipher.

### `char shiftUppercase(char ch,int offset)`

- **WHAT / contract:** Для A…Z циклический сдвиг на offset (может быть отрицательным). Остальные символы не менять. Используйте Math.floorMod.
- **Constraints:** Любой char и любой int offset, включая MIN_VALUE и MAX_VALUE. Требуемая сложность: O(1).
- **Examples:** shiftUppercase('Z',1) → A; shiftUppercase('A',-1) → Z; shiftUppercase('!',7) → !; shiftUppercase('Z',MAX_VALUE) → W.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `shiftUppercase`, не совпадающий с examples.
- **Typical pitfalls:** Сначала нормализуйте offset: ch+offset может переполниться. Отрицательный % — не floorMod.

### `String encode(String text,int offset)`

- **WHAT / contract:** Применить shiftUppercase к каждому char, собрать строку.
- **Constraints:** text не null; любой offset; меняются только латинские A..Z. Требуемая сложность: O(n).
- **Examples:** encode("AZ-я",1) → "BA-я"; encode("",7) → "".
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `encode`, не совпадающий с examples.
- **Typical pitfalls:** lowercase и Unicode-символы сохраняются; нормализация регистра здесь запрещена.


## Подсказка

<details><summary>Открыть после собственной попытки</summary>

Сложение до остатка переполняется; отрицание MIN_VALUE тоже не помещается в int. Начните с минимального входа, на котором два возможных объяснения поведения расходятся. Зафиксируйте его до реализации.

</details>

## Запуск и проверки

Из корня: ./mvnw -DskipTests package, затем java -jar app/target/java-games-app-1.0.0.jar 23.
На Windows используйте .\mvnw.cmd. Это компиляция и запуск демо, не приёмка.

Приёмка: ./mvnw -pl :captain-cipher -am verify. Незавершённые методы дают IncompleteExerciseException.
При запуске окна та же заглушка даёт намеренно неполноценное поведение. Частично реализованная игра использует ваши готовые методы; ошибки вашего кода не заменяются демо.
F5 сбрасывает сцену; после изменения кода нужна пересборка. В каталоге src/test/java — настоящие Jupiter-тесты.

## Самопроверка и защита

1. Как получить обратный сдвиг MIN_VALUE без -offset?
2. Какой ваш тест пройдёт у неверного решения и чем вы дополнили его?
3. Назовите предположение, вне которого ваш контракт ничего не обещает.

AI можно использовать для гипотез и review. В AI_USAGE.md укажите принятые/отклонённые предложения и независимую проверку (либо «не использовался»). На защите требуется объяснить контрпример и внести изменение самостоятельно.

## Дополнительно

После обязательной работы предложите одно изменение правил. До кода опишите, какие контракты оно затронет, и придумайте тест совместимости. Это добровольная часть.

## Путеводитель по коду

- [`CaesarCipher`](src/main/java/edu/course/games/captaincipher/domain/CaesarCipher.java): контракты и учебные методы, без UI.
- [`CaptainCipherGame`](src/main/java/edu/course/games/captaincipher/ui/CaptainCipherGame.java): перевод событий и анимация.
- [`CaptainCipherRenderer`](src/main/java/edu/course/games/captaincipher/ui/CaptainCipherRenderer.java): только рисование, без изменения состояния.
- `src/test/java/edu/course/games/captaincipher/domain`: примеры и граничные тесты, зеркалящие domain.

