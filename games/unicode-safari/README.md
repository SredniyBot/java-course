# 21. Unicode-сафари — Одно животное, несколько чисел

Счётчик показывает четыре символа там, где пользователь видит три.

## Что вы исследуете

char, кодовая точка и графема различаются; combining accent — отдельная кодовая точка.

Темы: UTF-16/UTF-8, бюджет, ограниченный контракт. Для доказательства корректности используйте понятия лекции 3: контракт, класс входов, независимый oracle, граница и регрессия. Формальные доказательства не требуются; требуется объяснить основание каждого важного assertion.

## Перед началом

Предыдущая работа: [20. Космическая кухня](../space-kitchen/README.md). Переносите её практику проверки, а не её код.

Работа 21/25. Ориентир 345–375 минут. До реализации согласуйте контракт нового API в DESIGN.md. Обязательны модель, интеграция, проверки отказов и разбор компромиссов.

## Обязательная работа: Бюджет сообщения

1. Запустите демо и опишите наблюдаемый дефект: действие → результат → ожидаемое поведение. Баннер показывает вызванные незавершённые методы, а не процент готовности.
2. Реализуйте базовые методы по контрактам ниже. Заглушку ExercisePreview.unfinished заменяйте целиком; не включайте preview внутри acceptance-тестов.
3. Добавьте TextBudget.analyze(text,maxBytes) для корректного Unicode и maxBytes>=0: codePoints, utf8Bytes, longestPrefix. Префикс самый длинный по кодовым точкам в бюджете UTF-8. Графемные кластеры не обещаются. Подключите показатели к UI.
4. Проверьте A🐱Б, e с combining acute, пустую строку и бюджеты до/на/после границы. Максимальность проверяет ещё одну точку, а не только размер результата.
5. Повторите исходный сценарий в окне. Отдельно проверьте F5, F6 и ошибочный ввод. Сохраните команду проверки и её результат.

## Что сдавать

- Код, собственные Jupiter-тесты и небольшой TEST_PLAN.md: класс входов → пример → ожидаемый результат → какую ошибку ловит.
- Один минимальный контрпример к правдоподобной неправильной реализации. Сначала покажите красный тест, затем исправление.
- REGRESSION.md: вход, причина дефекта, почему исправление устраняет причину, ограничения проверки.
- Новый domain API и его использование в UI; изолированный класс без подключения не завершает работу.
- DESIGN.md: инварианты, владельцы данных, rejected alternative и стоимость операций. Один новый change request выполните после review.
- Воспроизводимый сценарий из нескольких команд и независимая модель/оракул на ограниченном подмножестве входов.

## Приёмка

Базовые публичные тесты обязательны, но не исчерпывают работу. Новая часть оценивается по условиям пункта 3 и доказательствам пункта 4. Зелёные tests без обязательной новой функциональности не означают завершение. Нельзя ослаблять существующий контракт, подгонять expected или удалять проверки ради зелёного статуса.

## Контракты заданий
WHAT — требуемый результат; contract — поведение и изменения; constraints — допустимые входы. Предусловия задаёт вызывающий код: вне явно описанного домена поведение не специфицировано, скрытые тесты не требуют придуманной валидации. Null запрещён, кроме явно разрешённых элементов history. Входы не изменять, если контракт прямо не требует мутацию. Нельзя использовать время, случайность, I/O или глобальное изменяемое состояние в UnicodeText.

### `int countCodePoints(String text)`

- **WHAT / contract:** Посчитать Unicode-кодовые точки, не char. Используйте codePointCount на всей строке.
- **Constraints:** text не null; корректный UTF-16. Графемные кластеры специально не считаем. Требуемая сложность: O(n).
- **Examples:** countCodePoints("A😀") → 2; countCodePoints("é") → 2; countCodePoints("") → 0.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `countCodePoints`, не совпадающий с examples.
- **Typical pitfalls:** char, code point и видимый символ — три разных понятия.

### `int utf8Length(String text)`

- **WHAT / contract:** Число байтов при кодировании UTF-8; кодировку задать явно.
- **Constraints:** text не null; корректный UTF-16; кодировка UTF-8 без BOM. Требуемая сложность: O(n).
- **Examples:** utf8Length("A😀") → 5; utf8Length("Я") → 2; utf8Length("") → 0.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `utf8Length`, не совпадающий с examples.
- **Typical pitfalls:** Не использовать defaultCharset; размер в памяти UTF-16 — не размер UTF-8.

### `String prefixByCodePoints(String text,int n)`

- **WHAT / contract:** Вернуть первые n кодовых точек, не разрывая суррогатные пары. Если n больше количества, вернуть всю строку; n>=0.
- **Constraints:** text корректный UTF-16, не null; n≥0. Требуемая сложность: O(text.length).
- **Examples:** prefixByCodePoints("A😀B",2) → "A😀"; n=0 → ""; n=99 → вся строка.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `prefixByCodePoints`, не совпадающий с examples.
- **Typical pitfalls:** substring(0,n) индексирует char; составная графема может разделиться по контракту.


## Подсказка

<details><summary>Открыть после собственной попытки</summary>

char, кодовая точка и графема различаются; combining accent — отдельная кодовая точка. Начните с минимального входа, на котором два возможных объяснения поведения расходятся. Зафиксируйте его до реализации.

</details>

## Запуск и проверки

Из корня: ./mvnw -DskipTests package, затем java -jar app/target/java-games-app-1.0.0.jar 21.
На Windows используйте .\mvnw.cmd. Это компиляция и запуск демо, не приёмка.

Приёмка: ./mvnw -pl :unicode-safari -am verify. Незавершённые методы дают IncompleteExerciseException.
При запуске окна та же заглушка даёт намеренно неполноценное поведение. Частично реализованная игра использует ваши готовые методы; ошибки вашего кода не заменяются демо.
F5 сбрасывает сцену; после изменения кода нужна пересборка. В каталоге src/test/java — настоящие Jupiter-тесты.

## Самопроверка и защита

1. Почему корректный префикс может оборвать графему?
2. Какой ваш тест пройдёт у неверного решения и чем вы дополнили его?
3. Назовите предположение, вне которого ваш контракт ничего не обещает.

AI можно использовать для гипотез и review. В AI_USAGE.md укажите принятые/отклонённые предложения и независимую проверку (либо «не использовался»). На защите требуется объяснить контрпример и внести изменение самостоятельно.

## Дополнительно

После обязательной работы предложите одно изменение правил. До кода опишите, какие контракты оно затронет, и придумайте тест совместимости. Это добровольная часть.

## Путеводитель по коду

- [`UnicodeText`](src/main/java/edu/course/games/unicodesafari/domain/UnicodeText.java): контракты и учебные методы, без UI.
- [`UnicodeSafariGame`](src/main/java/edu/course/games/unicodesafari/ui/UnicodeSafariGame.java): перевод событий и анимация.
- [`UnicodeSafariRenderer`](src/main/java/edu/course/games/unicodesafari/ui/UnicodeSafariRenderer.java): только рисование, без изменения состояния.
- `src/test/java/edu/course/games/unicodesafari/domain`: примеры и граничные тесты, зеркалящие domain.

