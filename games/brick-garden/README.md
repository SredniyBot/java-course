# 05. Кирпичный сад

Разбейте все 24 кирпича.

## Learning outcomes
Полуоткрытые интервалы и обход матрицы. Java: Двумерные массивы, поиск, счётчики. После работы студент умеет объяснить контракт и подтвердить его тестом, а не только показать работающий экран.

## Prerequisites
Базовый синтаксис Java, методы и запуск Maven. Предыдущие игры/знания: 01. Уровень 1/3, ориентир 60–75 минут без установки JDK.

## Milestones
1. Нарисуйте два граничных примера на бумаге.
2. Реализуйте первый метод по описанию; прогоните тесты.
3. Реализуйте остальные методы, добавьте тест на границу.
4. Проверьте обычный ход и перезапуск окна.

## Контракты заданий
WHAT — требуемый результат; contract — поведение и изменения; constraints — допустимые входы. Предусловия задаёт вызывающий код: вне явно описанного домена поведение не специфицировано, скрытые тесты не требуют придуманной валидации. Null запрещён, кроме явно разрешённых элементов history. Входы не изменять, если контракт прямо не требует мутацию. Нельзя использовать время, случайность, I/O или глобальное изменяемое состояние в BrickRules.

### `int columnAt(double x)`

- **WHAT / contract:** Кирпичи начинаются с x=80, шаг 100, ширина 94, всего 8. Вернуть индекс или -1, включая промежутки.
- **Constraints:** x конечен, abs ≤ 10^6. Кирпич c занимает [80+100*c,174+100*c), c=0..7. Требуемая сложность: O(1).
- **Examples:** columnAt(80) → 0; columnAt(173.9) → 0; columnAt(174) → -1; columnAt(180) → 1; columnAt(874) → -1.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `columnAt`, не совпадающий с examples.
- **Typical pitfalls:** Промежуток — не кирпич; приведение отрицательного double к int не заменяет floor.

### `int countRemaining(int[][] board)`

- **WHAT / contract:** Посчитать элементы >0; массив прямоугольный, не null.
- **Constraints:** Ненулевой объект массива, строки не null; прямоугольный, в том числе 0 строк или 0 столбцов. Требуемая сложность: O(h*w).
- **Examples:** countRemaining([[1,0],[-1,2]]) → 2; countRemaining([]) → 0.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `countRemaining`, не совпадающий с examples.
- **Typical pitfalls:** Ненулевое отрицательное значение не считается кирпичом; вход не изменять.

## Hints
<details><summary>Открыть подсказку</summary>

Сначала разберите примеры вручную; реализуйте формулу/условие буквально, затем проверьте точную границу. Не сокращайте код до одной строки, если это мешает объяснению.

</details>

## Tests и запуск
Из корня репозитория: `./mvnw -pl :brick-garden -am test`. Windows: `.\mvnw.cmd -pl :brick-garden -am test`.
Проверки находятся в `src/test/java/edu/course/games/brickgarden/domain`. Это Jupiter, не отдельный main.
После полной сборки `./mvnw verify` запустите `java -jar app/target/java-games-app-1.0.0.jar 5`.
В IDE запустите `edu.course.app.Launcher` с аргументом `5`. F5 сбрасывает игру, F6 ставит на паузу; код после правки нужно перекомпилировать.
В студенческой выдаче тесты изначально красные из-за незаполненных методов. Это ожидаемо; отключать их нельзя.

## Optional extension / запрос на защите
Сделать ширину и зазор кирпичей настраиваемыми.
Сохраните существующие контракты через адаптер либо документируйте согласованное изменение и обновите UI с тестами. Полная эталонная версия закрывает базовые контракты; этот запрос студент выполняет самостоятельно.

## AI policy / check questions
Можно использовать AI для объяснения API, поиска гипотез и критики тестов. В `AI_USAGE.md` укажите инструмент, задачу, что принято и как независимо проверено (или «не использовался»). Не засчитывается тест, чей expected просто скопирован из реализации.

1. Объясните `columnAt` на входе, которого нет в примерах.
2. Какой объект изменяется, кто ещё может держать ссылку на него?
3. Какой тест упадёт при ошибке: Промежуток — не кирпич; приведение отрицательного double к int не заменяет floor.
4. Выполните небольшой change request без повторной генерации всего класса.

## Путеводитель по коду

- [`BrickRules`](src/main/java/edu/course/games/brickgarden/domain/BrickRules.java): контракты и учебные методы, без UI.
- [`BrickGardenGame`](src/main/java/edu/course/games/brickgarden/ui/BrickGardenGame.java): перевод событий и анимация.
- [`BrickGardenRenderer`](src/main/java/edu/course/games/brickgarden/ui/BrickGardenRenderer.java): только рисование, без изменения состояния.
- `src/test/java/edu/course/games/brickgarden/domain`: примеры и граничные тесты, зеркалящие domain.
