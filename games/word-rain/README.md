# 10. Словопад — Словарь без региональных сюрпризов

На машине с другой локалью те же клавиши дают другой ответ. Повторный ввод выдаёт очки дважды.

## Что вы исследуете

Нормализация — отдельная граница; префикс и полное совпадение имеют разные последствия.

Темы: String, Locale.ROOT, идемпотентность. Для доказательства корректности используйте понятия лекции 3: контракт, класс входов, независимый oracle, граница и регрессия. Формальные доказательства не требуются; требуется объяснить основание каждого важного assertion.

## Перед началом

Предыдущая работа: [09. Котики-близнецы](../memory-cats/README.md). Переносите её практику проверки, а не её код.

Работа 10/25. Ориентир 180–210 минут. Кроме базовых методов обязательна новая модель с тестами и подключением к UI. Не складывайте состояние в static.

## Обязательная работа: Одна награда на слово

1. Запустите демо и опишите наблюдаемый дефект: действие → результат → ожидаемое поведение. Баннер показывает вызванные незавершённые методы, а не процент готовности.
2. Реализуйте базовые методы по контрактам ниже. Заглушку ExercisePreview.unfinished заменяйте целиком; не включайте preview внутри acceptance-тестов.
3. Добавьте WordAttempt: хранит нормализованное целевое слово; submit(text) возвращает PREFIX, MATCH или MISS. Первый MATCH завершает попытку, последующие submit возвращают ALREADY_COMPLETED без очков. Модель независима от default locale. Подключите UI.
4. Проверьте пустой префикс, суффикс, I в турецкой локали и два MATCH подряд. Изменённую локаль в тесте восстановите в finally.
5. Повторите исходный сценарий в окне. Отдельно проверьте F5, F6 и ошибочный ввод. Сохраните команду проверки и её результат.

## Что сдавать

- Код, собственные Jupiter-тесты и небольшой TEST_PLAN.md: класс входов → пример → ожидаемый результат → какую ошибку ловит.
- Один минимальный контрпример к правдоподобной неправильной реализации. Сначала покажите красный тест, затем исправление.
- REGRESSION.md: вход, причина дефекта, почему исправление устраняет причину, ограничения проверки.
- Новый domain API и его использование в UI; изолированный класс без подключения не завершает работу.

## Приёмка

Базовые публичные тесты обязательны, но не исчерпывают работу. Новая часть оценивается по условиям пункта 3 и доказательствам пункта 4. Зелёные tests без обязательной новой функциональности не означают завершение. Нельзя ослаблять существующий контракт, подгонять expected или удалять проверки ради зелёного статуса.

## Контракты заданий
WHAT — требуемый результат; contract — поведение и изменения; constraints — допустимые входы. Предусловия задаёт вызывающий код: вне явно описанного домена поведение не специфицировано, скрытые тесты не требуют придуманной валидации. Null запрещён, кроме явно разрешённых элементов history. Входы не изменять, если контракт прямо не требует мутацию. Нельзя использовать время, случайность, I/O или глобальное изменяемое состояние в WordRules.

### `String normalizeInput(String text)`

- **WHAT / contract:** Убрать пробелы по краям и привести к нижнему регистру с Locale.ROOT.
- **Constraints:** text не null; используются правила String.strip и Locale.ROOT, внутренние пробелы сохраняются. Требуемая сложность: O(n).
- **Examples:** normalizeInput("  JAVA  ") → "java"; normalizeInput("A  B") → "a  b"; normalizeInput("") → "".
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `normalizeInput`, не совпадающий с examples.
- **Typical pitfalls:** trim и strip различаются; регистр не должен зависеть от системной локали.

### `boolean isPrefix(String word,String input)`

- **WHAT / contract:** Вернуть true, если word начинается с input. Пустой input является префиксом.
- **Constraints:** word и input не null; сравнение регистрозависимое, дополнительной нормализации нет. Требуемая сложность: O(input.length).
- **Examples:** isPrefix("java","") → true; isPrefix("java","jav") → true; isPrefix("java","Java") → false.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `isPrefix`, не совпадающий с examples.
- **Typical pitfalls:** Присутствие подстроки не означает префикс; пустая строка — префикс.


## Подсказка

<details><summary>Открыть после собственной попытки</summary>

Нормализация — отдельная граница; префикс и полное совпадение имеют разные последствия. Начните с минимального входа, на котором два возможных объяснения поведения расходятся. Зафиксируйте его до реализации.

</details>

## Запуск и проверки

Из корня: ./mvnw -DskipTests package, затем java -jar app/target/java-games-app-1.0.0.jar 10.
На Windows используйте .\mvnw.cmd. Это компиляция и запуск демо, не приёмка.

Приёмка: ./mvnw -pl :word-rain -am verify. Незавершённые методы дают IncompleteExerciseException.
При запуске окна та же заглушка даёт намеренно неполноценное поведение. Частично реализованная игра использует ваши готовые методы; ошибки вашего кода не заменяются демо.
F5 сбрасывает сцену; после изменения кода нужна пересборка. В каталоге src/test/java — настоящие Jupiter-тесты.

## Самопроверка и защита

1. Почему startsWith недостаточно для награды?
2. Какой ваш тест пройдёт у неверного решения и чем вы дополнили его?
3. Назовите предположение, вне которого ваш контракт ничего не обещает.

AI можно использовать для гипотез и review. В AI_USAGE.md укажите принятые/отклонённые предложения и независимую проверку (либо «не использовался»). На защите требуется объяснить контрпример и внести изменение самостоятельно.

## Дополнительно

После обязательной работы предложите одно изменение правил. До кода опишите, какие контракты оно затронет, и придумайте тест совместимости. Это добровольная часть.

## Путеводитель по коду

- [`WordRules`](src/main/java/edu/course/games/wordrain/domain/WordRules.java): контракты и учебные методы, без UI.
- [`WordRainGame`](src/main/java/edu/course/games/wordrain/ui/WordRainGame.java): перевод событий и анимация.
- [`WordRainRenderer`](src/main/java/edu/course/games/wordrain/ui/WordRainRenderer.java): только рисование, без изменения состояния.
- `src/test/java/edu/course/games/wordrain/domain`: примеры и граничные тесты, зеркалящие domain.

