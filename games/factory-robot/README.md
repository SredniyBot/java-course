# 25. Робот на заводе — Программа, которую можно остановить

Вложенные повторы не должны замораживать UI; текущая команда должна быть видна.

## Что вы исследуете

Парсинг, ограничения ресурсов и исполнение — разные этапы; ошибка не перемещает робота частично.

Темы: Parser, AST, композиция, бюджет исполнения. Для доказательства корректности используйте понятия лекции 3: контракт, класс входов, независимый oracle, граница и регрессия. Формальные доказательства не требуются; требуется объяснить основание каждого важного assertion.

## Перед началом

Предыдущая работа: [24. Баллистическая почта](../ballistic-mail/README.md). Переносите её практику проверки, а не её код.

Работа 25/25. Ориентир 405–435 минут. До реализации согласуйте контракт нового API в DESIGN.md. Обязательны модель, интеграция, проверки отказов и разбор компромиссов.

## Обязательная работа: Мини-язык робота

1. Запустите демо и опишите наблюдаемый дефект: действие → результат → ожидаемое поведение. Баннер показывает вызванные незавершённые методы, а не процент готовности.
2. Реализуйте базовые методы по контрактам ниже. Заглушку ExercisePreview.unfinished заменяйте целиком; не включайте preview внутри acceptance-тестов.
3. Создайте parser/executor для F/L/R и repeat n { program }, n=1..100, вложенность<=8, развёрнутая длина<=10000. Пробелы между токенами допустимы, регистр строгий. Ошибка содержит позицию UTF-16; parser не меняет мир. step выполняет одну примитивную команду; pause/resume/reset и immutable trace. Подключите UI.
4. Сдайте грамматику, AST/API, лимиты, тесты malformed/пустого блока/глубины/длины/крайнего n, модельное сравнение коротких программ и pause→resume→reset. На защите добавьте команду без переписывания parser целиком.
5. Повторите исходный сценарий в окне. Отдельно проверьте F5, F6 и ошибочный ввод. Сохраните команду проверки и её результат.

## Что сдавать

- Код, собственные Jupiter-тесты и небольшой TEST_PLAN.md: класс входов → пример → ожидаемый результат → какую ошибку ловит.
- Один минимальный контрпример к правдоподобной неправильной реализации. Сначала покажите красный тест, затем исправление.
- REGRESSION.md: вход, причина дефекта, почему исправление устраняет причину, ограничения проверки.
- Новый domain API и его использование в UI; изолированный класс без подключения не завершает работу.
- DESIGN.md: инварианты, владельцы данных, rejected alternative и стоимость операций. Один новый change request выполните после review.
- Воспроизводимый сценарий из нескольких команд и независимая модель/оракул на ограниченном подмножестве входов.

## Приёмка

Базовые публичные тесты обязательны, но не исчерпывают работу. Новая часть оценивается по условиям пункта 3 и доказательствам пункта 4. Зелёные tests без обязательной новой функциональности не означают завершение. Нельзя ослаблять существующий контракт, подгонять expected или удалять проверки ради зелёного статуса.

## Контракты заданий
WHAT — требуемый результат; contract — поведение и изменения; constraints — допустимые входы. Предусловия задаёт вызывающий код: вне явно описанного домена поведение не специфицировано, скрытые тесты не требуют придуманной валидации. Null запрещён, кроме явно разрешённых элементов history. Входы не изменять, если контракт прямо не требует мутацию. Нельзя использовать время, случайность, I/O или глобальное изменяемое состояние в RobotRules.

### `int nextDirection(int direction,char command)`

- **WHAT / contract:** 0 вправо,1 вниз,2 влево,3 вверх. R поворачивает по часовой стрелке, L — против. F сохраняет направление.
- **Constraints:** direction 0..3; command F,L,R. Требуемая сложность: O(1).
- **Examples:** nextDirection(0,'L') → 3; nextDirection(3,'R') → 0; nextDirection(2,'F') → 2.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `nextDirection`, не совпадающий с examples.
- **Typical pitfalls:** Остаток отрицательного числа может быть -1; F не меняет направление.

### `Cell nextCell(int[][] map, int row, int col, int direction)`

- **WHAT / contract:** Вернуть Cell(row, col) следующей клетки, если она внутри map и равна 0; иначе Cell с прежними координатами. map не менять.
- **Constraints:** map непустая прямоугольная; row,col внутри; direction 0..3; 0=проход, любое другое значение=преграда. Требуемая сложность: O(1).
- **Examples:** nextCell([[0,0]],0,0,0) → Cell(0,1); nextCell([[0,1]],0,0,0) → Cell(0,0).
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `nextCell`, не совпадающий с examples.
- **Typical pitfalls:** row и col не x/y; при запрете хода возвращаются прежние координаты; map неизменна.

### `boolean isValidProgram(String program)`

- **WHAT / contract:** Непустая строка содержит только F,L,R. Регистр уже нормализован интерфейсом.
- **Constraints:** program не null; регистр уже может быть нормализован UI, но метод сам ничего не нормализует. Требуемая сложность: O(n).
- **Examples:** isValidProgram("FFRFL") → true; isValidProgram("") → false; isValidProgram("flr") → false; isValidProgram("F F") → false.
- **Acceptance criteria:** все приведённые примеры воспроизводятся; границы включены/исключены точно по контракту; результат и допустимые изменения входов проверены отдельными assertions. Добавьте минимум один собственный случай для `isValidProgram`, не совпадающий с examples.
- **Typical pitfalls:** Пустая программа невалидна; Character.isLetter пропускает лишние команды.


## Подсказка

<details><summary>Открыть после собственной попытки</summary>

Парсинг, ограничения ресурсов и исполнение — разные этапы; ошибка не перемещает робота частично. Начните с минимального входа, на котором два возможных объяснения поведения расходятся. Зафиксируйте его до реализации.

</details>

## Запуск и проверки

Из корня: ./mvnw -DskipTests package, затем java -jar app/target/java-games-app-1.0.0.jar 25.
На Windows используйте .\mvnw.cmd. Это компиляция и запуск демо, не приёмка.

Приёмка: ./mvnw -pl :factory-robot -am verify. Незавершённые методы дают IncompleteExerciseException.
При запуске окна та же заглушка даёт намеренно неполноценное поведение. Частично реализованная игра использует ваши готовые методы; ошибки вашего кода не заменяются демо.
F5 сбрасывает сцену; после изменения кода нужна пересборка. В каталоге src/test/java — настоящие Jupiter-тесты.

## Самопроверка и защита

1. Где проверяется длина программы и как не переполнить счётчик?
2. Какой ваш тест пройдёт у неверного решения и чем вы дополнили его?
3. Назовите предположение, вне которого ваш контракт ничего не обещает.

AI можно использовать для гипотез и review. В AI_USAGE.md укажите принятые/отклонённые предложения и независимую проверку (либо «не использовался»). На защите требуется объяснить контрпример и внести изменение самостоятельно.

## Дополнительно

После обязательной работы предложите одно изменение правил. До кода опишите, какие контракты оно затронет, и придумайте тест совместимости. Это добровольная часть.

## Путеводитель по коду

- [`RobotRules`](src/main/java/edu/course/games/factoryrobot/domain/RobotRules.java): контракты и учебные методы, без UI.
- [`FactoryRobotGame`](src/main/java/edu/course/games/factoryrobot/ui/FactoryRobotGame.java): перевод событий и анимация.
- [`FactoryRobotRenderer`](src/main/java/edu/course/games/factoryrobot/ui/FactoryRobotRenderer.java): только рисование, без изменения состояния.
- `src/test/java/edu/course/games/factoryrobot/domain`: примеры и граничные тесты, зеркалящие domain.
- [`RobotRunner`](src/main/java/edu/course/games/factoryrobot/domain/RobotRunner.java): владелец игрового состояния, отдельно проверяемый без окна.

